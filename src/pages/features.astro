---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import FeaturesComponent from "../components/Features.astro";
import ScrollReveal from "../components/ScrollReveal.tsx";
import { SITE } from "../config/site.mjs";
---

<Layout
  title={`Features | ${SITE.title}`}
  description="Discover all the features included in Astro Theme Switcher: React, Framer Motion, Tailwind CSS, Astro Icons, Sass, MDX, and more."
  canonicalURL={`${SITE.url}/features`}
>
  <Header />
  <main id="main-content" role="main">
    <section class="max-w-6xl mx-auto px-4 py-20">
      <ScrollReveal client:idle>
        <h1 class="text-5xl font-bold mb-4" style="color: var(--token-color-text-primary, #111827);">Features</h1>
      </ScrollReveal>
      <ScrollReveal client:load delay={0.1}>
        <p class="text-xl mb-12" style="color: var(--token-color-text-muted, #4b5563);">
          Everything you need to build modern web experiences.
        </p>
      </ScrollReveal>
      <FeaturesComponent />
    </section>
  </main>
  <Footer />
</Layout>
